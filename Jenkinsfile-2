pipeline {
    agent any

    stages {
        stage('Node Test') {
            steps {
                script {
                    docker.image('node:16-alpine').inside {
                        sh 'node --version'
                    }
                }
            }
        }
    }
}